server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/parkingdb
    username: parkuser
    password: parksecret
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
